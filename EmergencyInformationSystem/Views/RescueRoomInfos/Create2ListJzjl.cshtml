@model IEnumerable<EmergencyInformationSystem.Models.ViewModels.RescueRoomInfos.Create.ItemJzjl>

<table class="table table-bordered table-condensed table-hover">
    <thead>
        <tr>
            <th class="text-center" width="15%">
                @Html.DisplayNameFor(model => model.FirstDoctorName)
            </th>
            <th class="text-center" width="65%">
                @Html.DisplayNameFor(model => model.DiagnosisNameOrigin)
            </th>
            <th class="text-center" width="15%">
                @Html.DisplayNameFor(model => model.ReceiveTime)
            </th>
            <th width="5%">
            </th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td class="text-center">
                    @Html.DisplayFor(model => item.FirstDoctorName)
                </td>
                <td class="text-center">
                    @Html.DisplayFor(model => item.DiagnosisNameOrigin)
                </td>
                <td class="text-center">
                    @Html.DisplayFor(model => item.ReceiveTime)
                </td>
                <td class="text-center">
                    @Html.ActionLink("选择", "Create3", new { outPatientNumber = item.OutPatientNumber, KDJID = item.ORIGIN_KDJID, BRXXID = item.ORIGIN_BRXXID, GHXXID = item.ORIGIN_GHXXID, JZID = item.ORIGIN_JZID }, new { @class = "label label-default" })
                </td>
            </tr>
        }
    </tbody>
</table>