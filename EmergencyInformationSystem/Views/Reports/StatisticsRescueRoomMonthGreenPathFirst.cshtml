@model IEnumerable<EmergencyInformationSystem.Models.ViewModels.Reports.StatisticsRescueRoomMonth.GreenPathFirst>

<table class="table table-bordered table-hover table-condensed">
    <thead>
        <tr>
            <th class="text-center alert-success">
                绿色通道
            </th>
            <th class="text-center alert-success" style="width:15%">
                数量
            </th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.ActionLink(item.IsGreenPathName, "IndexRescueRoomGreenPath", new { item.IsGreenPath, item.Time, item.Level }, new { target = "_blank" })
                </td>
                <td>
                    @Html.DisplayFor(model => item.Count)
                </td>
            </tr>

            @Html.Partial("StatisticsRescueRoomMonthGreenPathSecond", item.List)
        }
    </tbody>
</table>