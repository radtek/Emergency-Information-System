@model IEnumerable<IGrouping<Guid, EmergencyInformationSystem.Models.Domains.Entities.RescueRoomTreatmentRecord>>

<table class="table table-bordered table-hover table-condensed">
    <thead>
        <tr>
            <th class="text-center">
                @Html.DisplayNameFor(model => model.First().PrescriptionTime)
            </th>
            <th class="text-center">
                @Html.DisplayNameFor(model => model.First().ProductCode)
            </th>
            <th class="text-center">
                @Html.DisplayNameFor(model => model.First().ProductName)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            @Html.Partial("IndexPartialPart", item)
        }
    </tbody>
</table>