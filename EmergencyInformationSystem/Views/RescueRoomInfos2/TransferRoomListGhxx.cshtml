@model IEnumerable<EmergencyInformationSystem.Models.ViewModels.RescueRoomInfos2.TransferRoom.ItemGhxx>

@foreach (var item in Model)
{
    <span>
        @Html.DisplayNameFor(model => model.RegisterTime)
        ：
        @Html.DisplayFor(model => item.RegisterTime)
    </span>

    @Html.Partial("TransferRoomListJzjl", item.ListJzjl)

    <hr />
}